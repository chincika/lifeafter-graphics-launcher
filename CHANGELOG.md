# 更新日志

## v2.1.0 — 120 FPS 档独立接管

- 新增“帧率解锁”菜单，可将原生 120 FPS 档映射为 180、240 或 300 FPS。
- 条件补丁只在传入值等于 120 时替换；25/30/40/50/60/90 等其他档位继续使用游戏原始逻辑。
- 启动页对 120 FPS 画质预设显示“配置 120 · 实际目标”，其他预设标签保持不变。
- 识别并可安全迁移旧版“全局强制 180/240”补丁。
- 写入前创建完整 NPK 事务备份，并建立 SHA-256 验证的官方原包基线。
- 应用或恢复成功后自动清理旧事务备份，仅保留最新 1 份；官方初始还原点永久保留。
- 新增“清理冗余备份”操作，手动清理前会完整验证官方还原点且绝不删除该还原点。
- 加入 NXPK v3 元数据锁、目标槽位哈希锁、槽外整包哈希校验、游戏运行锁和写入失败自动回滚。
- 提供一键恢复官方原始 120 FPS 逻辑与独立完整备份目录。
- 本功能不包含反作弊绕过，仅适用于已审查的当前游戏包体版本。

## v2.0.1 — 启动记录统计修复

- 同一游戏进程切换游戏 ID 时，自动结束旧账号会话并开始新账号会话。
- 游戏 ID 变化需连续检测两次才确认，避免窗口标题短暂变化造成错误拆分。
- 总运行时长改为所有游戏窗口运行区间的并集，多开重叠时段只计算一次。
- 单独运行主号或任意单一窗口时，运行区间仍会完整计入总时长。
- 同一账号同时出现在多个窗口时，账号时长也会去除重叠区间。
- 平均单次时长继续按独立账号会话计算，不受总时长去重影响。
- 自动将旧记录中的“游戏 ID + 服务器名”归一化为纯游戏 ID，避免同一账号在排行中重复出现。
- 重新打开启动器但游戏进程未重启时，不再重复增加账号启动次数。
- 账号排行与最近会话卡片固定显示 7 行，更多内容改为卡片内部滚动。

## v2.0.0 — 现代桌面重大更新

### 全新界面

- 重构为 Windows 11 Fluent 风格现代桌面应用。
- 加入 Mica/毛玻璃质感、分层卡片、统一图标与现代化交互状态。
- 保留便携式交付方式，无需安装即可运行。

### 实例监控

- 自动检测最多 4 个《明日之后》游戏进程。
- 从游戏窗口标题提取游戏 ID，并将其作为实例与账号名称。
- 实时展示分辨率、帧率、CPU、内存和运行时长。
- 将实例快照设计为独立数据契约，为后续局域网副屏监控预留扩展能力。

### 启动记录

- 按游戏 ID 自动建立并持久化启动会话。
- 支持日、周、月、全部四个统计周期。
- 展示总运行时长、启动次数、最常用账号、平均单次时长和账号排行。
- 展示最近启动会话，并支持导出 CSV、打开本地记录目录。
- 所有历史数据只保存在本机；游戏退出或关闭启动器时封存当前会话。

### 画质与工具

- “均衡画质”更新为 900p 120 FPS。
- 保留 2K、1080p、900p、720p、540p 等常用预设。
- 保留稳定多开、配置备份、恢复、日志和跳字缩放工具。
