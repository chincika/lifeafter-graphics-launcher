# 更新日志

## v2.5.2 — 2026-08-10 配置保护更新

- 画质预设改为字段级合并：切换档位时只修改 `pcconfig.resolution` 和 `qualityconfig.frame`。
- 保留用户在游戏内选择的画质、隐藏特效、TAA、DLSS、Reflex、垂直同步与全屏设置，同时自动保留未来新增字段。
- `keymap` 和 `half_infected_keymap` 不再受预设模板覆盖，始终沿用游戏当前配置。
- 档位识别改为仅依据分辨率和帧率，不再依赖 `render` 等画质字段。
- 当 `resolution` 或 `frame` 缺失、重复时中止写入，避免异常配置被误修改。
- 新增配置保留回归测试，覆盖 2026-08-06 后的新画质参数、按键映射和未知字段。

## v2.5.1 — 2026-08-06 包体兼容更新

- 适配 2026-08-06 更新后的老 PC 包体，新增 `SettingManager` 元数据档案与独立的 180/240/300 FPS 定长补丁。
- 后端现在会按 NXPK 目标记录自动选择 2026-07-26 或 2026-08-06 补丁组，继续兼容旧老 PC 包体和发烧平台包体。
- 对新包体完成官方 120 → 180 → 240 → 300 → 官方 120 的完整副本测试，恢复后整包 SHA-256 与更新后的官方文件完全一致。

## v2.5.0 — CPU 0 保留策略
- 启动记录时长超过 24 小时后继续使用累计小时显示，例如 35 小时 42 分，不再换算为天；桌面端与局域网远端页面保持一致。
- “全部核心”和“自定义”模式新增“为系统保留 CPU 0”实验性开关，按画质预设独立记忆。
- 开启后从游戏 CPU Set 与硬亲和性掩码中排除组 0 的逻辑处理器 CPU 0，其余核心策略保持不变。
- 主界面摘要、多开方案和实例监控统一显示“− CPU0”，确保界面描述与实际调度一致。

## v2.4.1 — 进程调度状态修复
- 修复高级配置选择“系统管理”后仍显示并应用旧核心策略的问题。
- 系统管理、全部核心、性能核心和能效核心现在点击后立即保存；启动前也会提交当前配置作为兜底。
- 修复侧边栏仍显示旧版 `v2.3` 的版本标识。

## v2.4.0 — 智能进程调度

- 每个画质预设分别记忆进程优先级与 CPU 核心策略，不再共用一套设置。
- 540p 25 挂机档默认使用“低”优先级与能效核心；高帧主力预设默认使用“高”优先级与全部核心。
- 通过 Windows CPU Set 拓扑读取处理器型号、物理核心、逻辑处理器编号与 EfficiencyClass，不根据型号字符串或固定编号猜测大小核。
- 在单处理器组系统上同时写入进程相关性掩码，使任务管理器的核心勾选与预设策略保持一致；多处理器组系统安全回退为 CPU Set。
- 新增进程调度二级配置入口，支持系统管理、性能核心、能效核心和自定义逻辑处理器。
- 仅在识别到启动器新启动的真实游戏进程后应用策略，并校验进程路径属于当前游戏目录。

## v2.3.8 — 回归稳定版

- 功能与界面整体恢复至仓库迁移前的 v2.3.0：保留双包体、包体菜单、赞赏入口、性能模式、帧率接管和控件遮挡修复。
- 移除迁移后的混淆核心、签名、公私钥及其相关发布结构。
- 保留新版自动更新的下载复用、SHA-256 校验、Windows Shell 独立替换、失败回滚和缓存清理。
- 保留最小化到托盘仅隐藏窗口、不销毁进程的修复。
- 后端与帧率补丁启动时校验并复制到版本隔离的稳定目录，避免便携版重复启动破坏临时资源。

- 新增顶部“游戏包体”菜单：集中展示多个安装目录，可在“老PC包体”和“发烧平台包体”之间安全切换。
- 启动时自动扫描固定磁盘中的 `LifeAfter` 与 `mrzh` 目录，同时支持手动添加任意位置的游戏目录。
- 游戏目录列表会持久记录来源、版本、最后使用时间和功能能力；失效记录可单独移除，且不会删除任何游戏文件。
- 包体版本从游戏自身的 `release_version_config` 动态读取，不依赖写死的 `20260724` 版本号。
- “工具”页新增自愿赞赏区域，完整保留个人赞赏码识别范围，并明确标注赞赏不影响任何功能。
- 恢复现代版“性能优先”启动模式：启动时改用游戏官方 `Documents\bin\x64-3\lifeafter.exe` 通道，标准模式继续使用根目录 `lifeafter.exe`。
- 修复性能模式开关被画质预设下拉框遮挡、状态未传递到后端的问题。
- 新增性能通道可用性检测、选择记忆和多开联动；缺少 `x64-3` 文件时会阻止误启并提示修复游戏文件。
- 新增“发烧平台包体”自动识别与标准启动通道适配，原网易官方平台在界面中标记为“老PC包体”，支持当前 `20260724` 增量包的 120 FPS 档接管。
- 帧率写入严格限定为 `Documents\script.py314.lc.npk`；游戏根目录完整 NPK 只显示状态并永久保持只读。
- 游戏更新后会重新校验 NXPK v3、SettingManager 元数据和目标槽位：结构完全一致时自动建立新版本兼容档案，结构变化时锁定写入。
- 官方初始还原点改为按“安装位置 + 平台 + 包体档案”隔离，并存放在启动器数据区；游戏目录只保留可自动清理的事务备份。
- 启动记录页新增独立开关：关闭时立即结束当前计时段，实例监控继续工作，但暂停期间不会写入新的启动记录。
- 修复帧率解锁页顶部包体选择入口与“当前接管规则”提示重叠的问题。
- 新增 GitHub Release 自动更新：默认每次启动检查，也可改为每天、每周或每月检查。
- 新版本会以流式方式下载到启动器数据区，并在 SHA-256 校验通过后替换当前 Windows 便携版；游戏运行时会等游戏完全退出再更新。
- 更新替换带有旧版本临时回滚点：新程序无法正常启动时恢复原文件，成功启动后自动清理。

## v2.2.0 — 后台守护与局域网副屏

- 新增系统托盘常驻：最小化后销毁渲染窗口，仅保留监控、记录和托盘核心。
- 支持关闭按钮收至托盘、随 Windows 静默启动，以及从托盘彻底退出并停止记录。
- 实例监控迁移到主进程统一调度，按前台、游戏运行、远端连接和空闲状态自适应调整检测频率。
- 历史记录不再随每次实例轮询写入；会话变化立即保存，持续运行时每 60 秒安全落盘。
- 新增“后台与远程”页面，可开启局域网只读状态服务并查看服务、配对和设备授权状态。
- 远端页面支持手机、平板和桌面浏览器，实时查看游戏实例、CPU、内存、运行时长和启动记录。
- 状态推送使用轻量 SSE 单向通道；桌面界面和远端页面共用同一份实例快照，不重复扫描游戏进程。
- 新设备需使用五分钟有效的六位配对码连接；每台设备使用独立凭证，可随时单独或全部撤销。
- 局域网接口严格只读，不提供启动游戏、修改画质、修改帧率或读取本机文件的远程操作。
- 远端数据隐藏游戏目录、备份路径和包体信息；服务默认关闭且不包含公网转发、UPnP 或云端中转。

## v2.1.0 — 120 FPS 档独立接管

- 新增“帧率解锁”菜单，可将原生 120 FPS 档映射为 180、240 或 300 FPS。
- 条件补丁只在传入值等于 120 时替换；25/30/40/50/60/90 等其他档位继续使用游戏原始逻辑。
- 启动页对 120 FPS 画质预设显示“配置 120 · 实际目标”，其他预设标签保持不变。
- 识别并可安全迁移旧版“全局强制 180/240”补丁。
- 写入前创建完整 NPK 事务备份，并建立 SHA-256 验证的官方原包基线。
- 应用或恢复成功后自动清理旧事务备份，仅保留最新 1 份；官方初始还原点永久保留。
- 新增“清理冗余备份”操作，手动清理前会完整验证官方还原点且绝不删除该还原点。
- 加入 NXPK v3 元数据锁、目标槽位哈希锁、槽外整包哈希校验、游戏运行锁和写入失败自动回滚。
- 提供一键恢复官方原始 120 FPS 逻辑与独立完整备份目录。
- 本功能不包含反作弊绕过，仅适用于已审查的当前游戏包体版本。

## v2.0.1 — 启动记录统计修复

- 同一游戏进程切换游戏 ID 时，自动结束旧账号会话并开始新账号会话。
- 游戏 ID 变化需连续检测两次才确认，避免窗口标题短暂变化造成错误拆分。
- 总运行时长改为所有游戏窗口运行区间的并集，多开重叠时段只计算一次。
- 单独运行主号或任意单一窗口时，运行区间仍会完整计入总时长。
- 同一账号同时出现在多个窗口时，账号时长也会去除重叠区间。
- 平均单次时长继续按独立账号会话计算，不受总时长去重影响。
- 自动将旧记录中的“游戏 ID + 服务器名”归一化为纯游戏 ID，避免同一账号在排行中重复出现。
- 重新打开启动器但游戏进程未重启时，不再重复增加账号启动次数。
- 账号排行与最近会话卡片固定显示 7 行，更多内容改为卡片内部滚动。

## v2.0.0 — 现代桌面重大更新

### 全新界面

- 重构为 Windows 11 Fluent 风格现代桌面应用。
- 加入 Mica/毛玻璃质感、分层卡片、统一图标与现代化交互状态。
- 保留便携式交付方式，无需安装即可运行。

### 实例监控

- 自动检测最多 4 个《明日之后》游戏进程。
- 从游戏窗口标题提取游戏 ID，并将其作为实例与账号名称。
- 实时展示分辨率、帧率、CPU、内存和运行时长。
- 将实例快照设计为独立数据契约，为后续局域网副屏监控预留扩展能力。

### 启动记录

- 按游戏 ID 自动建立并持久化启动会话。
- 支持日、周、月、全部四个统计周期。
- 展示总运行时长、启动次数、最常用账号、平均单次时长和账号排行。
- 展示最近启动会话，并支持导出 CSV、打开本地记录目录。
- 所有历史数据只保存在本机；游戏退出或关闭启动器时封存当前会话。

### 画质与工具

- “均衡画质”更新为 900p 120 FPS。
- 保留 2K、1080p、900p、720p、540p 等常用预设。
- 保留稳定多开、配置备份、恢复、日志和跳字缩放工具。
