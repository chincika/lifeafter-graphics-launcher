# 更新日志

## v2.3.6

- 修复启动器最小化后主窗口被销毁、便携版进程可能直接退出的问题。
- 最小化和关闭到托盘时改为隐藏并保留原窗口实例。
- 点击托盘图标可恢复同一窗口，后台实例监测和启动时长记录保持运行。
- 此版本已通过自动托盘生命周期测试和实际使用验证。

## v2.3.5 更新链路测试版

- 仅将版本号提升至 v2.3.5，用于验证 v2.3.4 的自动下载、退出、Shell 独立调度、替换、重启和缓存清理闭环。
- 更新器实现与 v2.3.4 相同，不包含额外功能变更。

## v2.3.4

- 修复便携版退出时更新工作进程被一并终止，导致下载完成但无法替换的问题。
- 更新工作进程改由 Windows Shell 独立拉起，脱离旧启动器的进程树。
- 增加工作进程启动、替换结果和失败原因日志。
- 保留文件占用重试、SHA-256 校验、新版本启动失败回滚及缓存清理。

## v2.3.3 更新链路测试版

- 仅将版本号提升至 v2.3.3，用于验证 v2.3.2 的自动下载、退出、替换、重启和缓存清理闭环。
- 更新器实现与 v2.3.2 相同，不包含额外功能变更。

## v2.3.2

- 修复便携版外层进程仍占用旧 EXE 时自动替换失败的问题。
- 更新替换增加最长 120 秒的文件占用重试。
- 已下载且 SHA-256 正确的安装包会直接复用，不再重复下载。
- 扩大更新回滚范围，新程序启动失败时恢复旧 EXE。
- 新增更新结果与失败日志，并自动清理旧版本缓存、残留脚本和过期临时文件。

## v2.3.1

- 修复游戏退出后帧率应用、恢复与清理按钮不会自动恢复可用的问题。
- 游戏运行期间显示明确的安全锁定原因，全部实例退出后自动刷新包体状态。
- 移除程序内的 Ed25519 公钥、签名验证和签名构建机制，更新改用 GitHub 资产摘要与多阶段 SHA-256 校验。
- 保留并重新验证原生帧率核心的随机混淆、补丁资源加密、核心完整性校验和 Electron 安全熔断。

## v2.3.0

- 新增老 PC 包体与发烧平台包体识别和多目录管理。
- 恢复并完善性能启动模式。
- 新增启动记录开关、托盘后台记录与局域网浏览器副屏。
- 新增启动自动检查更新、检查频率设置和便携版自动替换。
- 帧率接管支持 180 / 240 / 300 FPS，保留永久官方初始还原点。
- 自动清理冗余事务备份，手动清理同样不会删除永久还原点。
- 将帧率/NPK 核心迁移到混淆后的本机模块，补丁资源加密内置。
- 启用 Electron ASAR 完整性校验并关闭 Node/调试注入入口。
- 自动更新新增多阶段 SHA-256 校验。

历史版本已从公开发行区移除。迁移前的完整仓库与发行资产已由维护者离线留档。
