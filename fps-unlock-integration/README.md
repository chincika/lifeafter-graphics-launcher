# 帧率解锁集成补丁

这些补丁仅适用于已审查的 2026-07-26 `script.py314.lc.npk` 版本。

- 原生 120 FPS：按所选目标替换为 180 / 240 / 300 FPS。
- 其他传入值：继续执行游戏原始逻辑，不做修改。
- `patch_original.bin`：官方原始 `SettingManager` 槽位，用于恢复。

补丁采用完整包体版本锁、目标记录元数据锁和槽位哈希锁。启动器还会在写入前创建完整
NPK 备份，并在写入校验失败时自动回滚。
